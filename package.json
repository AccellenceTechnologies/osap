{
  "name": "osap",
  "version": "1.0.0",
  "description": "Open Secure Alarm Protocol (OSAP) – Open, secure JSON-over-HTTPS specification and schema",
  "license": "Apache-2.0",
  "scripts": {
    "validate:schema": "node validate.js",
    "lint:openapi": "redocly lint openapi.yaml",
    "build:openapi": "redocly bundle openapi.yaml -o dist/openapi.bundle.yaml",
    "ci:all": "npm run validate:schema && npm run lint:openapi && npm run build:openapi",
    "precommit": "npm run validate:schema && npm run lint:openapi"
  },
  "devDependencies": {
    "@redocly/cli": "^1.14.0",
    "ajv-cli": "^5.0.0",
    "ajv-formats": "^3.0.0",
    "husky": "^9.0.0",
    "glob": "^11.0.0"
  }
}
